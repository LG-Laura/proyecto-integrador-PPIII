
// let currentIndex = 0; // Índice de la imagen actual
// const items = document.querySelectorAll('.tech-item'); // Selecciona todos los ítems

// function showNextImage() {
//     items[currentIndex].style.display = 'none'; // Oculta la imagen actual
//     currentIndex = (currentIndex + 1) % items.length; // Incrementa el índice y reinicia si es necesario
//     items[currentIndex].style.display = 'flex'; // Muestra la siguiente imagen
// }

// // Inicialmente oculta todas las imágenes excepto la primera
// items.forEach((item, index) => {
//     if (index !== 0) {
//         item.style.display = 'none';
//     }
// });

// // Cambia la imagen cada 3 segundos
// setInterval(showNextImage, 1500);
